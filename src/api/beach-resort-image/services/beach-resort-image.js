'use strict';

/**
 * beach-resort-image service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::beach-resort-image.beach-resort-image');
